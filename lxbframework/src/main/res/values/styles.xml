<resources>

    <style name="baseStyle" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/windowbackground</item>
        <item name="android:textColorHint">@color/textcolorhint</item>
        <item name="android:textColor">@color/textcolor</item>
    </style>
    <!-- 自定义dialog样式 -->
    <style name="custom_dialog_theme" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <!-- <item name="android:backgroundDimEnabled">false</item> -->
        <item name="android:windowContentOverlay">@null</item>
    </style>
    <!-- dialog 标题字体风格 -->
    <style name="wheel_dialog_TitleTextStyle">
        <item name="android:textSize">22sp</item>
        <!-- 大小 -->
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/dialog_blue</item>
        <!-- 颜色 -->
    </style>
    <!-- 图片多选 -->
    <style name="anim_popup_dir">
        <item name="android:windowEnterAnimation">@anim/slide_in</item>
        <item name="android:windowExitAnimation">@anim/slide_out</item>
    </style>
</resources>